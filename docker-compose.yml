services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: mallnav
      POSTGRES_PASSWORD: mallnav
      POSTGRES_DB: mallnav
    ports:
      - "5432:5432"
    volumes:
      - mallnav_pgdata:/var/lib/postgresql/data

volumes:
  mallnav_pgdata:
